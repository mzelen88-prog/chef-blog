{
  "name": "chef-blog",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch",
    "preview": "node preview/serve.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
